name: Run Arc PayLink exact mainnet settlement (completed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The one-time 0.01 USDC settlement pilot is complete and disabled."
