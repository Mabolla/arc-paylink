name: Create Arc PayLink walletless mainnet escrow (completed)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "The one-time 0.01 USDC walletless escrow pilot is complete and disabled."
